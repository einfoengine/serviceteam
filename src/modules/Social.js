import React from 'react'

const Social = ({title, paragrapg, link, imgSrc, btnLabel, alt, subTitle, className}) => {
    return (
        <div>
            
        </div>
    )
}

export default Social
